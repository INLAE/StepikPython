Напишите программу, которая cчитывает из консоли целые числа, пока не 
будет получена команда «stop», и, находит сумму всех введенных простых 
и сумму всех введенных непростых чисел. Если введено отрицательное 
число, игнорируйте его и не добавляйте его ни в одну из сумм, так как оно 
не может быть простым, и выводите сообщение «Number is negative.» и 
продолжайте считывать значения. 

def Prime(n):
    if n < 2:
        return False
    if n == 2:
        return True
    limit = (n**0.5)
    i = 2
    while i <= limit:
        if n % i == 0:
            return False
        i += 1
    return True

pr = 0
cm = 0

while True:
  n = input()
  if n == 'stop':
    break
  n = int(n)
  if n < 0:
    print('Number is negative.')
  else:
      if bool(Prime(n)):
        pr += n
      else:
        cm += n
print('Sum of all prime numbers is:' , pr)
print ('Sum of all non prime numbers is:', cm)
